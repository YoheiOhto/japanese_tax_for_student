# TODO — japanese_tax_for_student 実装・修正リスト

---

## 🔴 計算が壊れているバグ

- [ ] **iDeCo の上限が自営業前提になっている** — `ohto_simulate.py` の `IDECO_MAX_GETSU_SELF = 6.8` が全員に適用されているが、会社員（2号被保険者）の上限は月1.2万円。雇用形態で分岐する必要がある
- [ ] **国民健康保険が40〜64歳の介護分を計算していない** — `kokuho.py` の `kokumin_kenko_hoken_bunkyo()` は介護分（40〜64歳）を計算外にしている。対象年齢を引数で受け取って分岐させる
- [ ] **2026年以降の健康保険料率がハードコード** — `ohto_simulate.py` の `fiscal_year = 2026 if year >= 2026 else 2025` により、2027年以降も2026年の料率を使い続ける。年ごとに外部設定から読み込む構造にする

---

## 🟡 計算の正確性に関わる未実装機能

- [ ] **市区町村ごとの国民健康保険料率に対応する** — 現在は文京区固定。料率テーブルを設定ファイル（YAML/JSON）に分離し、区市町村を引数で指定できるようにする
- [ ] **入力バリデーションを追加する** — 負の収入、空のリスト、iDeCo と国民年金の合計が上限超過などをチェックし、エラーメッセージを出す
- [ ] **奨学金の課税区分を分ける** — 給付型（非課税）と貸与型（課税される場合あり）を区別するパラメータを追加する

---

## 🟢 学生が実際に使いたい未実装の控除・計算

- [ ] **扶養控除・配偶者控除を追加する** — 親の扶養に入っている学生や既婚者のケースを計算できるようにする（特に「親の扶養から外れる収入ライン」の可視化が学生にとって重要）
- [ ] **医療費控除を追加する** — 年間10万円超の医療費がある場合の控除
- [ ] **生命保険料控除を追加する** — 社会人になる前後に加入するケースが多い
- [ ] **源泉徴収額と実際の納税額の差分を出力する** — 「確定申告で戻ってくる金額 / 追納する金額」を計算して表示する。学生が最も知りたい数字

---

## 🔵 UX・出力の改善

- [ ] **サマリー出力を追加する** — 現在の JSON は25項目のフラットな dict で読みにくい。「手取り収入・控除合計・納税額・実効税率」を一目で見られるサマリーブロックを追加する
- [ ] **CLI インターフェースを追加する** — `argparse` で `python simulate.py --salary 200 --ideco 2` のように実行できるようにする。スクリプトを直接編集しなくてよくなる
- [ ] **ファイル保存時にパスを表示する** — `save_result()` がファイルを保存しても保存先が表示されない。`print(f"保存しました: {path}")` を追加する
- [ ] **グラフ出力を追加する** — 収入額に対する実効税率・手取り額の推移を折れ線グラフで出力する（特に「扶養外れライン」の可視化）

---

## ⚙️ コード品質

- [ ] **`simulate.py` と `ohto_simulate.py` の重複を解消する** — 2ファイルの約45%が同じコード。共通ロジックを `src/` に切り出す
- [ ] **マジックナンバーを定数化する** — 税率ブラケット（195, 330, 695...）、保険料率（0.0869, 0.0280...）、控除額（48, 95...）をすべて名前付き定数にして参照元を明記する
- [ ] **型ヒントを追加する** — `kokuho.py`・`jumin_zei.py` 等に型ヒントがない。`def func(income: float) -> float:` の形式で統一する
- [ ] **テストを書く** — 国税庁の計算例と照合する単体テストを `tests/` に追加する。特に税率ブラケット境界値（195万・330万・695万等）のケースを網羅する
- [ ] **`requirements.txt` を作成する** — 依存ライブラリが未定義。`jupyter`・`pandas` 等を明記する

---

## 📋 ドキュメント

- [ ] **`archives/` フォルダに README を追加する** — 何が古くて何が現行なのかわからない
- [ ] **出力項目の説明を追加する** — `調整控除`・`基礎控除` など、税に詳しくないユーザーが結果を読めるように用語の説明を README または出力に含める
- [ ] **動作確認済みの年度を README に明記する** — 2024・2025・2026年の計算について、いつ・何を参照して実装したかを記録する
